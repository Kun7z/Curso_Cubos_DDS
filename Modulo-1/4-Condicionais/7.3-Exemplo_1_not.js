const ingresso = true;

if (!tem_ingresso) {  //analisa negativamente se isso é falso para prosseguir para a proxima linha de execução.
    console.log("Pessoa sem ingresso.")  //executa caso esteja positivo a negação de cima.
}