const saldo = 10;

const unidade = saldo === 1 ? "real" : "reais";


console.log(`Você tem ${saldo} ${unidade}`)  // se for plural vai utilizar reais, se for singular, no caso o número 1, vai usar real.


