//Faça um programa que imprima todos os elementos de um array que contém alguns números quaisquer.

let numeros = [0, 3, 18, 2, 11];

let indice = 0;

while (indice < numeros.length) {
    console.log(numeros[indice])
    indice++;
}